# Pinned to the venv used for paper submission.
# The submission environment already has these installed.
# This file is a record, not a fresh-install spec.

torch>=2.7
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
matplotlib>=3.8
transformers>=4.40   # Qwen3-Embedding-0.6B (lang emb)
sentencepiece
tqdm
